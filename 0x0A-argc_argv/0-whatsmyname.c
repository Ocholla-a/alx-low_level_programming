#include <stdio.h>
#include "main.h"

/**
 * main - code execution starts
 * @argc: number of command arguments passed
 * @argv: array of command arguments passed
 * Return: 0
 */

int main(int argc __attribute__ ((unused)), char **argv)
{
	printf ("%s\n", argv[0]);
	return (0);
}
